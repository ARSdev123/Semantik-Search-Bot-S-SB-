from fastapi import FastAPI
from pydantic import BaseModel
from chatbotcol import Chatbot

# Create FastAPI instance
app = FastAPI()
#creation de l'instance Chatbot au demarrage
#bot = Chatbot()

#class MessageRequest(BaseModel):
 #   message: str

#Endpoint pour repondre
#@app.post("/repondre")
#def repondre(q: MessageRequest):
 #   reponse = bot.repondre(q.message)
  #  return {"reponse": reponse}

@app.get("/")
def lire_racine():
    return {"message": "Bienvenue sur l'API du chatbot ! Utilisez le endpoint /repondre pour interagir."}   
